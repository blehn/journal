= form_for @post do |f|
  = render 'shared/error_messages', :object => f.object
  %span.timestamp
    #{Time.now.strftime("%B %d %Y %I:%M %p")}
  .field
    = f.text_area :content
  .actions
    = f.submit "Submit"